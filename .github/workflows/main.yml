name: 下载美股每年前1000只
on:
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install "longbridge==0.2.77" pandas tqdm

      - name: 运行 Step1 生成股票名单
        env:
          LP_APP_KEY: ${{ secrets.LP_APP_KEY }}
          LP_APP_SECRET: ${{ secrets.LP_APP_SECRET }}
          LP_ACCESS_TOKEN: ${{ secrets.LP_ACCESS_TOKEN }}
        run: python step1_build_top1000.py

      - name: 检查 Step1 输出
        run: |
          echo "=== top1000_by_year.csv 行数 ==="
          wc -l top1000_by_year.csv
          echo "=== 前5行 ==="
          head -5 top1000_by_year.csv

      - name: 运行 Step2 下载行情
        env:
          LP_APP_KEY: ${{ secrets.LP_APP_KEY }}
          LP_APP_SECRET: ${{ secrets.LP_APP_SECRET }}
          LP_ACCESS_TOKEN: ${{ secrets.LP_ACCESS_TOKEN }}
        run: python step2_download_ohlcv.py

      - name: 检查 Step2 输出
        run: |
          echo "=== 各年文件数量 ==="
          for year in 2021 2022 2023 2024 2025; do
            count=$(ls us_1000_turnover/$year/*.csv 2>/dev/null | wc -l)
            echo "$year 年：$count 个文件"
          done

      - name: 打包下载
        uses: actions/upload-artifact@v4
        with:
          name: us_1000_turnover_data
          path: us_1000_turnover/
